addconfig:
    description: "Add squid config"
    params:
        config-content:
            description: "Content of /etc/squid/squid.conf"
            type: string
            default: ""
start:
    description: "Start squid service"
restart:
    description: "Retart squid service"
stop:
    description: "Stop squid service"
